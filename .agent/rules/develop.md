---
trigger: always_on
---

# develop flow

- Install dependencies (`mise`, `mise install`, `moon init`)  
- start develop  
- preview change (`moon :dev`)  
- test change (`moon :lint`)  
- update CHANGELOG.md
- create PR

## package manager 
use `bun as package manager`